ver2.2

フラグを10個用意いたしました。

フラグのテキストボックスの文字列はその名前のフォルダーが生成されます。

一度も使われていないフラグのフォルダーは生成されません。

ドラッグ＆ドロップにも対応しています。

保存先は一つしか選べません。
保存先のディレクトリの下に、仕分けしたフォルダーが生成され、そこに画像が移動します。（コピーではない）

フォルダーでないものを選択すると例外が発生します。現在修正していません。